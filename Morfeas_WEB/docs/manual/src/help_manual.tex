\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{longtable}
\usepackage{array}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{parskip}
\usepackage{tabularx}
\usepackage{booktabs}

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=blue,
  pdftitle={LOG WEB Help Manual},
  pdfauthor={LOG Project}
}

\pagestyle{fancy}
\fancyhf{}
\lhead{LOG WEB}
\chead{Help Manual}
\rhead{\thepage}
\setlength{\headheight}{14pt}
\setlist[itemize]{leftmargin=1.4em}
\setlist[enumerate]{leftmargin=1.6em}

\titleformat{\section}{\large\bfseries}{}{0pt}{}
\titleformat{\subsection}{\normalsize\bfseries}{}{0pt}{}
\titleformat{\subsubsection}{\normalsize\bfseries}{}{0pt}{}

\newcommand{\messageexample}[2]{%
\noindent\textbf{Example Message (#1):}\\%
\begin{quote}\raggedright #2\end{quote}%
}

\title{LOG WEB\\Help Manual}
\author{}
\date{Current New Web UI}

\begin{document}
\maketitle
\thispagestyle{empty}
\tableofcontents
\newpage

\section{Introduction}
LOG WEB is the browser user interface for daily operation and configuration of the LOG system.
This manual describes the current \textbf{new} LOG WEB interface and is intended for operators and service users.

\subsection{Naming Note}
LOG WEB is the current product and user-interface name. \textbf{Morfeas WEB} is the legacy name.
Some internal paths, service names, repository names, protocol identifiers, and code comments may still use \texttt{Morfeas}, \texttt{Morfeas\_WEB}, or related forms for code compatibility and to avoid breaking existing deployments.
Operator-facing UI and current user documentation should use \textbf{LOG WEB}, except where an exact technical identifier must be shown.

Unless a site-specific deployment says otherwise, open the system in a browser with:

\begin{itemize}
\item \texttt{http://<device-ip>/}
\end{itemize}

This manual does not describe the retired legacy Morfeas WEB layout.

\section{Main Page}
The landing page is \textbf{ISO Channels Linker}. It is the main operational screen.

\subsection{What You See}
The page contains:

\begin{itemize}
\item a top bar with the FTP backup directory chip and rolling system summary,
\item a search bar,
\item action buttons for importing and adding channels,
\item the main ISO channel table,
\item a right-click context menu for channel actions,
\item a menu button that opens all secondary pages.
\end{itemize}

\subsection{Main Table}
The table shows the configured ISO channels and related runtime information.
Typical columns include:

\begin{itemize}
\item status,
\item ISO channel name,
\item device type,
\item connection anchor,
\item current value,
\item history,
\item next calibration,
\item minimum and maximum values,
\item description.
\end{itemize}

The page refreshes automatically while it is visible, so operators normally do not need to reload manually.

\subsection{Next Calibration}
The \textbf{Next Calibration} column is calculated from calibration metadata stored with each ISO channel.

\begin{itemize}
\item For \textbf{SDAQ} channels, calibration date and period come from the SDAQ runtime calibration data when the device reports it.
\item For \textbf{IOBOX}, \textbf{MTI}, and \textbf{NOX} channels, calibration metadata is stored in the OPC UA channel configuration as \texttt{CAL\_DATE} and \texttt{CAL\_PERIOD}.
\item When a non-SDAQ channel is added and no other calibration metadata is entered, the Add Channel popup defaults \texttt{CAL\_DATE} to the current date and \texttt{CAL\_PERIOD} to 12 months.
\item The displayed next calibration date is \texttt{CAL\_DATE + CAL\_PERIOD} months.
\item A missing or invalid calibration date or period is shown as \texttt{---}.
\end{itemize}

For example, a non-SDAQ channel added on \texttt{2026-05-11} with the default 12-month period is shown with next calibration \texttt{2027-05-11}.

\subsection{Measurement Display Rules}
The \textbf{Value} column follows the runtime measurement semantics of the current system.

\begin{itemize}
\item Normal valid measurements are shown as numeric values with the unit when a unit is available.
\item \textbf{Out of Range} and \textbf{Over Range} continue to show the actual runtime measurement value. They are not converted to an error code.
\item Reserved runtime error codes are shown as \textbf{red integers} in the \textbf{Value} column.
\item Reserved error codes are shown without units and without decimal formatting.
\item Reserved error codes are not collapsed to \texttt{---}.
\end{itemize}

\subsection{Reserved Measurement Error Codes}
The following reserved values may appear in the \textbf{Value} column, in OPC UA downstream consumers, and in connected tools that read the same measurement path.
These values are emitted by LOG core. The web interface displays them when they are present in runtime data; it does not create these numeric error codes from status text alone.

\begin{longtable}{|p{0.18\textwidth}|p{0.28\textwidth}|p{0.42\textwidth}|}
\hline
\textbf{Code} & \textbf{Meaning} & \textbf{Typical Situation} \\
\hline
\texttt{-901} & OFF-Line / Disconnected & Handler is alive, but an individual source, receiver, sensor, or channel is disconnected or intentionally unavailable. \\
\hline
\texttt{-902} & No Sensor & The device reports that no sensor is present on that measurement channel. \\
\hline
\texttt{-903} & Stall / Heating & SDAQ channel is not receiving fresh samples, or NOX is heating / not ready for valid measurement. \\
\hline
\texttt{-904} & Unclassified & Runtime data is invalid, but core cannot map it to a more specific condition. \\
\hline
\texttt{-905} & Unreachable / Unregistered & Device-level source is gone or unreachable. Examples: handler/source node missing, IOBOX/MTI Modbus transport failure, or retired MDAQ source. \\
\hline
\texttt{-906} & Standby / Heater off & Device is reachable but intentionally not measuring. Currently used for NOX heater-off state. \\
\hline
\texttt{-907} & Signal Invalid / Data Invalid & Data is arriving but failed validation, usually a telemetry signal-quality or link-layer problem. Currently used by MTI. \\
\hline
\end{longtable}

\subsection{Device Status, Web Value, and OPC UA Value}
The same reserved value can have a different operator-facing status depending on the device context. Read \textbf{Status} first for the reason, then \textbf{Value} for the numeric value sent to downstream consumers.

\textbf{Web Status} below is the linker-table \textbf{Status} text. \textbf{Web Value} comes from handler logstat files and usually represents interval data. \textbf{OPC UA mapped Value} refers to the linked ISO channel \textbf{Value} node used by downstream OPC UA clients.

\begingroup
\small
\setlength{\tabcolsep}{2pt}
\begin{longtable}{|p{0.09\textwidth}|p{0.20\textwidth}|p{0.15\textwidth}|p{0.16\textwidth}|p{0.30\textwidth}|}
\hline
\textbf{Device} & \textbf{Web Status / Situation} & \textbf{Web Value} & \textbf{OPC UA Value} & \textbf{Meaning} \\
\hline
SDAQ & Okay & actual value & actual value & Channel is valid and measuring normally. \\
\hline
SDAQ & Out of Range / Over Range & actual value & actual value & Channel reports a real measurement, but it is outside the expected range. \\
\hline
SDAQ & No sensor & \texttt{-902} & \texttt{-902} & SDAQ reports no sensor on the channel. \\
\hline
SDAQ & Stall & \texttt{-903} & \texttt{-903} & SDAQ is online, but fresh samples are not arriving. \\
\hline
SDAQ & Unclassified & \texttt{-904} & \texttt{-904} & SDAQ reports an invalid channel condition without a more specific mapping. \\
\hline
SDAQ & OFF-Line / source missing & \texttt{---} when no logstat value is available & \texttt{-905} fallback & Linked source node is missing; OPC UA still returns a numeric unavailable value. \\
\hline
IOBOX & Okay & actual value & actual value & Channel is valid and measuring normally. \\
\hline
IOBOX & Unreachable & \texttt{-905} & \texttt{-905} & IOBOX device-level Modbus transport is unreachable. \\
\hline
IOBOX & Disconnected & \texttt{-901} & \texttt{-901} & IOBOX handler/device is alive, but the RX/channel path is not responding. \\
\hline
IOBOX & Unconfigured & \texttt{---} & \texttt{-905} fallback if source node is missing & IOBOX device is reachable, but the linked RX/channel path is not present in runtime logstat. Check IOBOX configuration and wiring. \\
\hline
IOBOX & No sensor & \texttt{-902} & \texttt{-902} & IOBOX reports no sensor on the channel. \\
\hline
IOBOX & Unclassified & \texttt{-904} & \texttt{-904} & IOBOX reports an invalid condition without a more specific mapping. \\
\hline
MTI & Okay & actual value & actual value & Channel is valid and measuring normally. \\
\hline
MTI & Unreachable & \texttt{-905} & \texttt{-905} & MTI device-level Modbus transport is unreachable. \\
\hline
MTI & Disconnected & \texttt{-901} & \texttt{-901} & MTI handler/device is alive, but telemetry is not responding. \\
\hline
MTI & Radio Disabled / Disabled & \texttt{---} when logstat has no channel; otherwise \texttt{-901} if core stamped the retained source & \texttt{-901} when the retained source node remains; \texttt{-905} if source node is gone & MTI telemetry was intentionally disabled or reconfigured. Web does not invent a numeric value. \\
\hline
MTI & No sensor & \texttt{-902} & \texttt{-902} & MTI telemetry reports no sensor on the channel. \\
\hline
MTI & Signal Invalid & \texttt{-907} & \texttt{-907} & Packets are arriving, but telemetry data failed validation. \\
\hline
MTI & Error / Unclassified & \texttt{-904} & \texttt{-904} & MTI telemetry is online, but the data is invalid without a more specific mapping. \\
\hline
NOX & Okay & actual value & actual value & UniNOx sensor is active and measuring normally. \\
\hline
NOX & Handler not running, NOX role removed, or sensor never detected & \texttt{---} & \texttt{-905} fallback if ISO link exists & NOX source node is missing. Web reports missing logstat/runtime source; OPC UA reports missing mapped source. \\
\hline
NOX & OFF-Line after previously detected sensor timeout & \texttt{-901} & \texttt{-905} if source node was removed & Logstat records the timed-out sensor as offline; OPC UA may only see the missing source node. \\
\hline
NOX & Heater off & \texttt{-906} & \texttt{-906} & Sensor is active, but heater-off state makes measurement unavailable. \\
\hline
NOX & Heating / Not in temperature & \texttt{-903} & \texttt{-903} & Sensor is active, but it is not ready for valid measurement. \\
\hline
NOX & Unclassified / invalid sensor condition & \texttt{-904} & \texttt{-904} & Sensor is active, but the reported measurement is invalid. \\
\hline
\end{longtable}
\endgroup

\textbf{MTI Disabled note:} Disabled is an operator-selected radio mode, not a measurement fault. If MTI telemetry is disabled, the web shows \textbf{Status = Disabled} and leaves \textbf{Value} as \texttt{---} unless LOG core explicitly provides a numeric channel value. This keeps OPC UA/logstat measurement values owned by core and prevents the web interface from inventing error codes.

\textbf{NOX mapped-channel note:} NOX uses dynamic sensor nodes for UniNOx addresses. If the NOX handler is not running, the bus has been switched away from NOX, or a sensor address has never been detected, the web linker table may show \textbf{Status = OFF-Line} with \textbf{Value = ---} because no logstat runtime source exists. The OPC UA mapped \textbf{Value} may return \texttt{-905} for the same ISO link because the mapped source node is missing. These two displays describe the same missing-source condition from different layers.

\subsection{Reading Status Together with Value}
Operators should read the \textbf{Status} and \textbf{Value} columns together:

\begin{itemize}
\item \textbf{Status = Out of Range} or \textbf{Over Range} with a numeric \textbf{Value} means the device still reports a measurement, but the channel is flagged as outside the expected calibrated or device range.
\item \textbf{Status = No sensor}, \textbf{OFF-Line}, \textbf{Disconnected}, \textbf{Stall}, or \textbf{Unclassified} together with a red negative \textbf{Value} means the measurement is not valid and the code should be interpreted using the error-code table above.
\item \textbf{Status = Unconfigured} with \textbf{Value = ---} means the device is reachable, but the configured runtime channel path is not present.
\item \textbf{Status = Disabled} with \textbf{Value = ---} means the function is intentionally turned off, not failed.
\end{itemize}

\subsection{Search}
Use \textbf{Search Input} to filter channels in the current table.

\subsection{Add Channel Device Search}
The Add Channel popup uses device search to choose runtime source paths for new links.
Search results are limited to the selected device family and to paths that are not already linked in the current OPC UA channel configuration.

\begin{itemize}
\item \textbf{SDAQ}: search lists detected SDAQ channels that are not already linked. Single-channel manual offline anchors are allowed when needed.
\item \textbf{IOBOX}: search lists channels from IOBOX devices whose runtime connection status is \textbf{Okay}. A receiver marked \textbf{Disconnected} is not listed. Individual channels under an online receiver may still be listed even when their current status is \textbf{No sensor} or otherwise invalid, matching the current channel-selection contract.
\item \textbf{MTI}: search lists channels only when the MTI runtime connection status is \textbf{Okay} and the radio/telemetry mode provides channel data. Modes \textbf{TC4}, \textbf{TC8}, \textbf{TC16}, and \textbf{QUAD} expose their channel list. \textbf{RMSW/MUX} exposes detected \textbf{Mini\_RMSW} measurement channels. \textbf{Disabled} does not expose Add Channel candidates.
\item \textbf{NOX}: search lists \textbf{NOx} and \textbf{O2} entries for detected UniNOx sensor objects. The current measurement does not need to be valid; for example, heater-off, warming-up, or previously detected \textbf{OFF-Line} sensors may still be linked and controlled later. OFF-Line historical candidates are marked with \texttt{(OFF-Line)} in the search result.
\end{itemize}

The search popup and save validation both enforce device-family matching. IOBOX candidates are not accepted as MTI links, and MTI candidates are not accepted as IOBOX links, even if the physical equipment connected behind the device can be similar.

\subsection{Keyboard Shortcuts}
The following keyboard shortcuts and key actions are currently implemented in LOG WEB.

\subsubsection{Main ISO Channels Linker}
\begin{itemize}
\item \textbf{Delete}: delete the currently selected channel rows. A confirmation dialog is shown first.
\item \textbf{Ctrl+Z}: undo the most recent successful delete. Only one delete step is remembered.
\item \textbf{Escape}: close the main menu or an open column filter menu.
\end{itemize}

\subsubsection{Add Devices Popup}
\begin{itemize}
\item \textbf{Ctrl+S}: save the device currently being added when the add form is open.
\item \textbf{Delete}: remove the currently selected removable devices.
\item \textbf{Escape}: close the popup window.
\end{itemize}

\subsubsection{Add Channel Popup}
\begin{itemize}
\item \textbf{Ctrl+S}: save the current Add Channel form.
\item \textbf{Enter} in the ISO field: accept the typed ISO code as a custom manual ISO value.
\item \textbf{Enter} in the sensor path field: accept the typed device path when it matches an available search-pool candidate. Manual offline anchors are intended for SDAQ single-channel use only.
\item \textbf{Escape}: close the popup window.
\end{itemize}

\subsubsection{Edit Link / Replace Link Popup}
\begin{itemize}
\item \textbf{Ctrl+S}: save the current Edit or Replace form.
\item \textbf{Escape}: close the popup window.
\end{itemize}

\subsubsection{Other Popup Pages}
\begin{itemize}
\item \textbf{Escape}: close popups such as Advanced Settings, Backup \& Restore Channels, Check System Status, Network Configuration, Version, and system control pages.
\end{itemize}

\section{Menu Overview}
The main menu is grouped as follows.

\begin{longtable}{|p{0.28\textwidth}|p{0.62\textwidth}|}
\hline
\textbf{Menu Item} & \textbf{Purpose} \\
\hline
Network Configuration & Edit hostname and Ethernet IPv4 settings. \\
\hline
Add Devices & View current devices and add or remove supported devices. \\
\hline
Backup \& Restore Channels & Configure FTP backup target, create backups, and restore saved bundles. \\
\hline
Check System Status & View live runtime details, log files, and system journal output. \\
\hline
Advanced Settings & View MAC/CAN information, configure NTP server, and manage ISOstandard XML. \\
\hline
System Update & Check for available system updates, then update LOG WEB and LOG core when desired. \\
\hline
System Reboot & Reboot the device. \\
\hline
System Shutdown & Power off the device. \\
\hline
Version & Show current web and core version information. \\
\hline
Help & Download this manual as PDF. \\
\hline
\end{longtable}

\section{Network Configuration}
Open \textbf{Network Configuration} from the menu to edit hostname and Ethernet settings.

\subsection{Fields}
The page contains:

\begin{itemize}
\item \textbf{Hostname}
\item \textbf{Mode}: Static or DHCP
\item \textbf{Network IP}
\item \textbf{Gateway}
\item \textbf{DNS addr}
\end{itemize}

Preset buttons are available at the top of the page for quick loading of predefined values.

\subsection{Buttons}
\begin{itemize}
\item \textbf{Load Last}: reload the last confirmed configuration into the form.
\item \textbf{Set}: apply the current values.
\item \textbf{Close}: close the popup window.
\end{itemize}

\subsection{Operator Notes}
\begin{itemize}
\item If you change the device IP address, reconnect the browser to the new HTTP address after the change is applied.
\item DHCP mode disables manual IPv4 entry.
\item Presets are convenience values only. Review them before applying.
\end{itemize}

\messageexample{Progress}{Applying configuration: validating and writing system network settings...}
\messageexample{Success}{Configuration applied. If the IP changed, reconnect to the new device address in your browser.}
\messageexample{Failure}{Apply failed: <error details>}

\section{Add Devices}
Open \textbf{Add Devices} to manage the current device inventory.

\subsection{Page Layout}
The page has three main areas.

\begin{itemize}
\item \textbf{CAN Bus Setup}: shows each CAN bus, current mode, bitrate, state, detected device count, and available actions.
\item \textbf{Configured Devices}: shows XML-backed devices that can be removed or configured.
\item \textbf{Detected Devices}: shows auto-detected runtime devices, especially SDAQ devices found from logstat data.
\end{itemize}

The page refreshes while the popup is focused. Polling pauses during remove or role-change operations so the displayed state does not fight the operation in progress.

\subsection{SDAQ Operation Flow}
\begin{itemize}
\item SDAQ devices are auto-discovered from runtime \texttt{logstat} data.
\item They appear in \textbf{Detected Devices}; they are not manually added with the \textbf{Add...} form.
\item To use a CAN bus for SDAQ, use the \textbf{Switch to SDAQ} action in \textbf{CAN Bus Setup}. The system sets the SDAQ role and expected bitrate, restarts the LOG service, then waits for SDAQ runtime data to appear.
\item After SDAQ devices are detected, link individual SDAQ channels from the main page with \textbf{Add Channels}.
\item If a bus is in SDAQ mode but no SDAQ appears, check wiring, power, and CAN bitrate/state in the same table.
\end{itemize}

\subsection{NOX Operation Flow}
\begin{itemize}
\item NOX is managed by CAN bus role, not by the manual \textbf{Add...} form.
\item Use \textbf{Switch to NOX} on the target CAN bus to assign that bus to NOX operation. The transition changes the role/bitrate together and restarts the LOG service.
\item When a bus is in NOX mode, \textbf{Open NOX Config} opens the NOX configuration page for that bus.
\item Use the NOX configuration page for sensor setup and NOX-specific live controls.
\item To remove NOX from a bus, use \textbf{Switch to SDAQ}. When removing a configured NOX row, the UI may also ask whether to restore the bus to SDAQ.
\end{itemize}

\subsection{IOBOX Operation Flow}
\begin{enumerate}
\item Click \textbf{Add...}.
\item Select \textbf{IO-BOX}.
\item Enter \textbf{Device Name}, for example \texttt{IOBOX\_01}.
\item Enter the IOBOX IPv4 address.
\item Click \textbf{Save}.
\item Confirm the service restart warning.
\item Wait for the page to refresh and show the configured device runtime status.
\end{enumerate}

Behind the UI, saving an IOBOX writes the device into the LOG configuration XML and restarts the LOG service. Duplicate device names and duplicate IPv4 addresses are blocked.

\subsection{MTI Operation Flow}
\begin{enumerate}
\item Click \textbf{Add...}.
\item Select \textbf{MTI}.
\item Enter a \textbf{Device Name}; for MTI it must follow D-Bus element rules: letters, numbers, and underscore only, and it must not start with a digit.
\item Enter the MTI IPv4 address.
\item Click \textbf{Save}.
\item Confirm the service restart warning.
\item After the device is listed, use \textbf{Open MTI Config} from the configured row to manage MTI setup, telemetry mode, and MTI-specific controls.
\end{enumerate}

Behind the UI, saving an MTI writes the device into the LOG configuration XML and restarts the LOG service. The page compares configured name/IP with runtime MTI logstat data to show whether the device is connected.

\subsection{Remove Devices}
\begin{itemize}
\item Select one or more rows in \textbf{Configured Devices}.
\item Click \textbf{Remove} or press \textbf{Delete} while focus is not inside a form field.
\item Confirm the service restart warning.
\item The selected XML-backed devices are removed and the LOG service is restarted.
\end{itemize}

\subsection{Operator Notes}
\begin{itemize}
\item SDAQ rows in \textbf{Detected Devices} are auto-discovered and are not removable from this page.
\item Manual add currently supports IOBOX and MTI only. SDAQ is auto-discovered; NOX is controlled through CAN bus role switching.
\item If the page shows a legacy MDAQ warning, add/remove and CAN role actions are blocked until the legacy MDAQ XML configuration is removed manually.
\item Any add, remove, or CAN role transition may need 10--60 seconds because the LOG service restarts.
\end{itemize}

\section{Backup and Restore Channels}
Open \textbf{Backup \& Restore Channels} for FTP-based backup and restore.

\subsection{FTP Server Setup}
The page contains:

\begin{itemize}
\item \textbf{Host IP}: FTP server IPv4 address. The default expected server is \texttt{10.193.135.70}.
\item \textbf{Engine Number}: remote directory name for this engine/device group. It may contain letters, numbers, dot, underscore, and hyphen.
\item \textbf{Connect \& Fetch}: save the FTP configuration, test the FTP login, and load available backup files.
\item \textbf{Disconnect}: remove the saved FTP configuration and disable automatic backups.
\end{itemize}

FTP credentials are read from the device-side credential file and are not typed in the UI. When the connection is saved, the main page FTP chip shows the active backup directory.

\subsection{What Is Backed Up}
Each backup bundle contains the current OPC UA channel configuration XML and the LOG device configuration XML. The bundle is compressed, uploaded to the selected FTP engine directory, and stored as an \texttt{.mbl} file.

\subsection{Automatic Nightly Backup}
\begin{itemize}
\item The deployment installs a root cron job that runs the FTP backup script every night at \texttt{00:10}.
\item Automatic backup only runs when a valid FTP configuration exists.
\item If no FTP configuration exists, the backup script logs that no valid engine number is configured and does not change system state.
\item After a backup, the script uploads the backup logs to the same FTP engine directory when possible.
\end{itemize}

\subsection{Manual Backup}
\begin{enumerate}
\item Open \textbf{Backup \& Restore Channels}.
\item Confirm or enter the FTP server IP.
\item Enter the engine number.
\item Click \textbf{Connect \& Fetch}.
\item Wait for \textbf{Connected to FTP... Configuration saved}.
\item Click \textbf{Backup Now}.
\item Wait for the success message and confirm that the new \texttt{.mbl} file appears in the list.
\end{enumerate}

\subsection{Restore}
\begin{enumerate}
\item Connect to the FTP server and engine directory.
\item Select one backup file from the list.
\item Click \textbf{Restore Selected}.
\item Wait for the restore result message.
\item Reload the main page and verify the channel/device configuration.
\end{enumerate}

\begin{itemize}
\item Restore downloads the selected \texttt{.mbl} file from FTP.
\item The file must be a valid backup bundle.
\item Restore overwrites both the OPC UA channel configuration XML and the LOG device configuration XML from the bundle.
\item Backup files are listed newest-first, and old remote backups are pruned so the engine directory keeps up to 50 \texttt{.mbl} files.
\end{itemize}

\subsection{Operator Notes}
\begin{itemize}
\item Backup and restore both require a reachable FTP server.
\item If the server is unavailable, the page shows an error and no configuration is changed.
\item If two browser sessions edit the FTP configuration, the page detects the updated configuration and refreshes the backup list.
\end{itemize}

\messageexample{Connect Success}{Connected to FTP at 10.193.135.70. Configuration saved.}
\messageexample{Connect Failure}{Connection failed: FTP connect failed}
\messageexample{Backup Success}{Backup complete}
\messageexample{Restore Failure}{Restore failed: <error details>}

\section{Check System Status}
Open \textbf{Check System Status} to inspect runtime data and logs.

\subsection{Tabs}
The page has three tabs.

\subsubsection{Details}
This tab shows live system and bus information in tables. Depending on the device and services, values may include:

\begin{itemize}
\item CPU temperature,
\item uptime,
\item bus utilization,
\item bus error rate,
\item detected SDAQ count,
\item incomplete SDAQ count,
\item bus voltage,
\item bus amperage,
\item shunt temperature,
\item last calibration timestamps.
\end{itemize}

Temperature values shown by the current web interface are displayed in degrees Celsius. This includes CPU temperature, SDAQnet shunt temperature, IOBOX/MTI temperature channels, and NOX page shunt temperature.

\subsubsection{System Logs}
This tab is used for LOG application logger files.

\begin{itemize}
\item \textbf{Preview Logger}: choose one log file to preview.
\item \textbf{Reload}: reload the logger list.
\item \textbf{Export Logs}: select one or more log files and export them as one combined text file.
\end{itemize}

The preview area shows the selected file content in a terminal-style viewer.

\subsubsection{System Journal}
This tab is used for journal output from system services.

\begin{itemize}
\item \textbf{Scope}: choose all services, LOG system service, Apache2, or SSH. The current UI label for the LOG system service may still appear as \textbf{Morfeas System} until that compatibility label is renamed.
\item \textbf{Lines}: choose how many lines to load.
\item \textbf{Reload}: reload the journal view.
\item \textbf{Export Journal}: export the currently requested journal text.
\end{itemize}

\subsection{How to Use the Tabs}
\subsubsection{Details Tab}
Use this tab first when checking current runtime health.
If a service is active but values look stale, compare with the Logs or Journal tabs.

\subsubsection{System Logs Tab}
Use this tab for LOG component logs when you want application-level detail such as handler messages, update logs, backup logs, or runtime component reports.
To export logs, choose one or more files in the \textbf{Export Logs} multi-select, then click \textbf{Export Selected}. LOG WEB downloads the selected logger files as one combined text file. Use \textbf{Reload} if the file list looks stale.

\subsubsection{System Journal Tab}
Use this tab when the problem looks system-level rather than application-level, for example:

\begin{itemize}
\item service startup failures,
\item Apache problems,
\item SSH availability,
\item reboot or shutdown investigation.
\end{itemize}

To export journal output, choose the \textbf{Scope}, choose the number of \textbf{Lines}, click \textbf{Reload} if needed, then click \textbf{Export Journal}. The browser downloads the currently requested journal text as a \texttt{.txt} file.

\section{Advanced Settings}
Open \textbf{Advanced Settings} for machine information, NTP, and ISOstandard management.

\subsection{Machine Identifier}
Displays the MAC address.

\subsection{SDAQNet (CAN-IFs)}
Displays the current CAN interface information for \texttt{can0} and \texttt{can1}. These values are informational and read-only on this page.

\subsection{Time Synchronization}
The page shows the current NTP server and allows entering a new IPv4 NTP server. Use \textbf{Apply} to store the new value.

\messageexample{Apply Success}{Applied NTP server: 192.168.1.10}
\messageexample{Validation Error}{NTP server must be a valid IPv4 address.}

\subsection{ISOstandard}
This section lets the user:

\begin{itemize}
\item view the currently selected ISOstandard source,
\item select a different available ISOstandard file,
\item upload a new ISOstandard XML file,
\item review the loaded ISO point definitions in a table.
\end{itemize}

\section{System Update}
Open \textbf{System Update} to manage full system software updates.

\subsection{Current Behavior}
The page supports:

\begin{itemize}
\item checking whether an update is available,
\item applying the update,
\item deferring the update,
\item reloading the page after service recovery.
\end{itemize}

\subsection{Reminder Dot}
A red dot on the menu item indicates that the system has recorded an available update. The reminder stays visible until a later check confirms that no update is pending, or until a successful update clears it.

\subsection{Important Note}
The current update flow handles the full system update path shown in the progress messages. It checks and updates LOG WEB, applies web deployment steps, checks and downloads LOG core changes, builds and installs core when needed, runs core health checks, restarts services, and waits for the web service to become reachable again.

\messageexample{Update Available}{Update available. Click "Update Now" to apply, or "Update Later" to postpone.}
\messageexample{Update Success}{Update flow completed and web service is reachable. Refresh page (Ctrl+F5) to load latest files.}
\messageexample{Update Failed}{System update failed: <error details>}

\section{System Reboot and System Shutdown}
The system control pages provide single-confirmation actions for reboot and shutdown.

\subsection{System Reboot}
Use \textbf{Reboot Now} when you want to restart the device and return to service after boot. The page shows post-action guidance so the operator knows when to reconnect and refresh the browser.

\messageexample{Reboot Accepted}{Reboot command accepted. Wait about 60--90 seconds, then open the device URL again and refresh the browser.}

\subsection{System Shutdown}
Use \textbf{Shutdown Now} when you want to power off the device. After shutdown, the device must be powered on again manually before the web interface becomes available.

\messageexample{Shutdown Accepted}{Shutdown command accepted. Device will go offline. Power it on manually when needed.}

\section{Version}
The \textbf{Version} page shows version and commit information for:

\begin{itemize}
\item the LOG WEB repository,
\item the LOG core repository,
\item the related commit dates.
\end{itemize}

Use this page when confirming what software is installed on a device.

\section{Import Channels}
Use \textbf{Import Channels} on the main page to create channels from a JSON file.

\subsection{How Import Opens}
Clicking \textbf{Import Channels} opens the browser file picker. Select a \texttt{.json} file; there is no separate import popup page.

\subsection{Expected File Format}
The file must contain a JSON array. Each entry must include:

\begin{itemize}
\item \texttt{ISO\_CHANNEL}
\item \texttt{INTERFACE\_TYPE}
\item \texttt{ANCHOR}
\end{itemize}

Optional fields include \texttt{DESCRIPTION}, \texttt{MIN}, \texttt{MAX}, alarm fields, \texttt{UNIT}, \texttt{CAL\_DATE}, and \texttt{CAL\_PERIOD}. For SDAQ imports, LOG WEB ignores \texttt{UNIT}, \texttt{CAL\_DATE}, and \texttt{CAL\_PERIOD} because SDAQ calibration data is read from the SDAQ calibration path.

\subsection{Typical Import Flow}
\begin{enumerate}
\item Prepare or export a JSON array of channel entries.
\item Click \textbf{Import Channels}.
\item Select the JSON file.
\item LOG WEB validates the file and maps each entry to the channel-create payload.
\item Valid entries are created one by one.
\item The main table refreshes after successful imports.
\end{enumerate}

\subsection{Validation and Conflicts}
\begin{itemize}
\item The import fails immediately if the file is not valid JSON or is not a JSON array.
\item Entries missing required fields are skipped and reported.
\item Duplicate ISO channels inside the import file are rejected.
\item Duplicate device paths inside the import file are rejected.
\item Existing live ISO-channel or device-path conflicts are rejected before creating that entry.
\item If some entries succeed and others fail, LOG WEB reports both the number imported and the failed entries.
\end{itemize}

\subsection{Export as Import Source}
The right-click \textbf{Export} action creates a JSON file in the same structure expected by import. This is the safest way to move selected channel definitions between devices, then review the file before importing it elsewhere.

\section{Right-Click Channel Options}
Right-click one or more channel rows in the main table to access channel operations.

\subsection{Selection Rules}
\begin{itemize}
\item With one selected row, LOG WEB can show row-specific actions.
\item With two or more selected rows, LOG WEB shows only \textbf{Delete} and \textbf{Export}.
\item If no row is selected and you right-click a row, that row is selected automatically.
\item If several rows are already selected and you right-click one of them, the multi-row selection is kept.
\end{itemize}

\subsection{Action Availability by Scenario}
\begin{longtable}{|p{0.24\textwidth}|p{0.66\textwidth}|}
\hline
\textbf{Scenario} & \textbf{Available actions} \\
\hline
Single SDAQ row & Edit, Delete, Calibration, Export. \\
\hline
Single SDAQ-I or SDAQ-U row & Edit, Delete, Scale, Calibration, Export. \\
\hline
Offline SDAQ row & Replace is added. \\
\hline
Offline complete SDAQ-TC16 group & Replace TC16 is added when the selected source belongs to a full CH1--CH16 offline TC16 group. \\
\hline
Single IOBOX row & Edit, Delete, Export. \\
\hline
Single MTI row & Edit, Delete, Export. \\
\hline
Single NOX row & Edit, Delete, Export. \\
\hline
Multiple rows & Delete, Export. \\
\hline
\end{longtable}

\subsection{Edit}
\begin{enumerate}
\item Right-click one channel row.
\item Choose \textbf{Edit}.
\item Review or change ISO code, description, min/max, alarm fields, unit, calibration date/period when shown, and device path.
\item Click \textbf{Save}.
\end{enumerate}

Edit uses the current channel data from the backend when available. The save operation updates the existing channel link.

\subsection{Delete}
\begin{enumerate}
\item Select one or more rows.
\item Right-click and choose \textbf{Delete}, or press \textbf{Delete}.
\item Confirm the delete prompt.
\end{enumerate}

After a successful delete, one undo step is remembered for a short time. Press \textbf{Ctrl+Z} to restore the last deleted channel set when there is no conflict with existing live channels.

\subsection{Export}
\begin{enumerate}
\item Select one or more rows.
\item Right-click and choose \textbf{Export}.
\item The browser downloads a timestamped \texttt{LOG\_ISOChannel\_Linker\_Export\_Selection} JSON file.
\end{enumerate}

The exported JSON can be used later with \textbf{Import Channels}. For SDAQ rows, export keeps the channel link fields; SDAQ device calibration remains owned by the SDAQ calibration data.

\subsection{Scale}
Scale is available only for \textbf{SDAQ-I} and \textbf{SDAQ-U} rows.

\begin{enumerate}
\item Right-click an SDAQ-I or SDAQ-U row.
\item Choose \textbf{Scale}.
\item Review \textbf{Current Measurement Value}.
\item Enter \textbf{Measurement Input Low}, \textbf{Measurement Input High}, \textbf{Engineering Output Low}, \textbf{Engineering Output High}, and \textbf{Engineering Unit}.
\item Review \textbf{Scaled Output Value}.
\item Click \textbf{Save}.
\item Use \textbf{Continue in Calibration} when you want to add or review calibration points for the same channel after saving the quick scale.
\end{enumerate}

The scale popup writes a two-point calibration table into the selected SDAQ channel calibration data. It is a quick workflow for SDAQ-I and SDAQ-U; the full Calibration popup edits the same underlying calibration table.

\subsection{Calibration}
Calibration is available for SDAQ rows.

\subsubsection{Purpose}
The calibration popup is a channel calibration point editor for the selected SDAQ channel. It is used to read, inspect, preview, and save the device calibration table.

\subsubsection{Page Layout}
The popup contains:

\begin{itemize}
\item \textbf{Upper Section}: device information, calibration metadata, and summary panel share the top area.
\item \textbf{Current Preview}: \texttt{Measured raw value}, \texttt{Current Device Output}, and \texttt{Preview Output}. Each value has a copy button when the value is available.
\item \textbf{Point Table}: \texttt{Uncalibrated value}, \texttt{Reference value}, \texttt{Offset}, \texttt{Gain}, \texttt{C2}, and \texttt{C3}.
\item \textbf{Preview Columns}: \texttt{Calibrated value} and \texttt{Difference (Calibrated - Reference)}.
\item \textbf{Summary Panel}: calibration type, slope or gain summary, offset, reference minimum and maximum, and unit.
\end{itemize}

\subsubsection{Buttons and Validation}
\begin{itemize}
\item \textbf{Read from SDAQ}: reload calibration directly from the device.
\item \textbf{Discard Changes}: restore the last data loaded into the popup.
\item \textbf{Save Metadata}: available when existing calibration is protected. It saves only calibration date and period, without changing points or coefficients.
\item \textbf{Edit Calibration Points}: unlocks point editing for a channel that already has calibration data. Confirming this action converts the editable table to auto-linear points and may overwrite existing coefficients.
\item \textbf{Save to SDAQ}: validate the current auto-linear point form and write it to the device.
\item \textbf{CH's Unit} must be selected from the supported runtime SDAQ unit list.
\item \textbf{Used Points} must be an integer between zero and the device maximum.
\item In point editing mode, \texttt{Uncalibrated value} and \texttt{Reference value} must contain finite numbers.
\item \texttt{Measure} values of used points must be unique. Rows can be entered in any order; LOG WEB follows the industry convention of letting the user enter rows by \texttt{Reference} (forward, reverse-response, or non-monotonic sensors are all supported) and sorts internally by \texttt{Measure} before computing coefficients and writing to the SDAQ.
\item \texttt{Reference value} may increase or decrease, depending on the sensor and engineering range.
\end{itemize}

\texttt{Offset}, \texttt{Gain}, \texttt{C2}, and \texttt{C3} are shown for transparency. In normal point editing mode they are read-only and LOG WEB recalculates them from the calibration points before saving. The goal of each used point is that \texttt{Difference (Calibrated - Reference)} is as close to zero as possible.

\subsection{Replace}
Replace is shown only for offline SDAQ rows. It uses the Edit Link popup in replace mode.

\begin{enumerate}
\item Right-click the offline SDAQ channel you want to replace.
\item Choose \textbf{Replace}.
\item The popup opens with the source type locked.
\item Use device search to choose the replacement source path.
\item Review ISO code, description, min/max, alarms, unit, and any shown calibration fields.
\item Click \textbf{Save}.
\end{enumerate}

Family mismatch is blocked. If the source SDAQ subtype is known, subtype mismatch is blocked. If the source subtype is unknown, replacement is allowed but the UI warns the operator to replace within the same type.

\messageexample{Warning}{Subtype unknown, make sure replace within same type.}
\messageexample{Blocked Replace}{Subtype mismatch: expected SDAQ-TC1, got SDAQ-AI8.}

\subsection{Replace TC16}
Replace TC16 is shown only when the selected source belongs to a resolvable offline SDAQ-TC16 group that is complete as channels CH1 through CH16.

\begin{enumerate}
\item Right-click any channel in the full offline TC16 source group.
\item Choose \textbf{Replace TC16}.
\item LOG WEB selects all 16 source channels.
\item The Replace TC16 popup shows the source group summary and channel mapping.
\item Choose one eligible unlinked TC16 target device.
\item Confirm the operation.
\item All 16 source channels are remapped to the target device in one atomic operation.
\end{enumerate}

\begin{itemize}
\item Replace TC16 is all-or-nothing.
\item If any validation fails, no channel mapping is changed.
\item Target selection is at device-group level, not one channel at a time.
\item If no eligible target is listed, there is currently no fully free TC16 target device available.
\end{itemize}

\messageexample{Success}{Replace TC16 complete: 16 channels updated.}
\messageexample{Failure}{Failed to apply TC16 replace. [tc16\_target\_not\_unlinked]}

\section{Troubleshooting}
This section gives quick operator guidance for common problems.

\subsection{The Web Page Does Not Open}
\begin{itemize}
\item Confirm that you are using the current device IP address over HTTP.
\item If the IP was recently changed, reconnect to the new address.
\item If the device was rebooted or updated, wait for services to come back and refresh the browser.
\item If still unavailable, check \textbf{System Journal} or use SSH to verify Apache and LOG services.
\end{itemize}

\subsection{FTP Backup Cannot Connect}
\begin{itemize}
\item Check that the FTP server IP is reachable from the device.
\item Confirm that the engine number is correct.
\item If the page says configuration was removed, reconnect first.
\item If the internal FTP server is currently unavailable, backup and restore cannot be tested end-to-end.
\end{itemize}

\subsection{No Backup Files Are Listed}
\begin{itemize}
\item Verify that the selected engine directory contains backup files.
\item Confirm that the FTP configuration belongs to the expected engine number.
\item Reload after reconnecting to the FTP server.
\end{itemize}

\subsection{Replace or Replace TC16 Option Is Missing}
\begin{itemize}
\item Confirm that the selected row supports that replace mode.
\item For Replace TC16, confirm that the source is a complete offline TC16 group with channels CH1 through CH16.
\item If the source group is partial, use normal single-channel replace instead.
\end{itemize}

\subsection{No Eligible TC16 Target Is Listed}
\begin{itemize}
\item No full unlinked TC16 target device is currently available.
\item If channels on the target are already linked, the target is blocked.
\item Refresh the popup after any device or link changes.
\end{itemize}

\subsection{Logs or Journal Look Empty}
\begin{itemize}
\item Use \textbf{Reload} once to refresh the selected tab.
\item Verify that the correct logger file or journal scope is selected.
\item If the service has not produced messages yet, the view may be temporarily empty.
\end{itemize}

\subsection{Red Negative Measurement Codes Are Shown}
\begin{itemize}
\item \texttt{-901} means the handler is alive, but the individual source, receiver, sensor, or channel is offline or disconnected.
\item \texttt{-902} means no sensor is present.
\item \texttt{-903} means the channel is stalled, heating, or not ready for valid measurement.
\item \texttt{-904} means the runtime data is invalid without a more specific mapping.
\item \texttt{-905} means the device-level source is unreachable or the mapped OPC UA source node is unregistered.
\item \texttt{-906} means standby / heater off.
\item \texttt{-907} means signal invalid / data invalid.
\item When these codes are shown, the measurement should not be interpreted as a physical live value.
\end{itemize}

\subsection{Update Reminder Dot Is Still Visible}
\begin{itemize}
\item The reminder is based on the stored update flag.
\item Run \textbf{Check Again} in the System Update page to refresh the latest status.
\item After a successful update or a clean re-check, the reminder should clear.
\end{itemize}

\section{Help PDF Maintenance}
The Help menu downloads a PDF file from:

\begin{itemize}
\item \texttt{docs/help\_manual.pdf}
\end{itemize}

The maintained source for this file is located under:

\begin{itemize}
\item \texttt{Morfeas\_WEB/docs/manual/}
\end{itemize}

This keeps the active user manual next to the current LOG WEB code instead of inside the archived legacy documentation tree.

\section{Legacy Documentation Status}
The old documentation tree under \texttt{LOG-web/Docs/Morfeas\_WEB\_Docs} is retained only as historical reference. Its structure and API descriptions belong to the legacy Morfeas WEB implementation and should not be used as the active operator manual for the current LOG WEB UI.

\end{document}
